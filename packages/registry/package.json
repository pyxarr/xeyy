{
  "name": "@xeyy/registry",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "pnpm exec tsc --noEmit",
    "generate:schemas": "node src/regenerate-schemas.ts",
    "lint": "eslint .",
    "test": "vitest run",
    "validate:registry": "node src/validate-registry.ts",
    "build:registry": "node src/build-registry.ts"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@xeyy/eslint-config": "workspace:*",
    "@xeyy/typescript-config": "workspace:*",
    "eslint": "10.9.1",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "zod": "^4.5.4"
  }
}