{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm build:registry",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/r/:path*",
      "destination": "/registry/:path*"
    }
  ]
}